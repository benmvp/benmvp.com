language: node_js
node_js:
  - "node"

cache: yarn

before_deploy:
  - yarn run build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: gatsby
  local-dir: public
  target-branch: master
